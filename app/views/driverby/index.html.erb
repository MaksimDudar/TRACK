<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="p-3 mb-2 bg-info text-dark bg-opacity-25">
  <h3>Список BY водителей</h3></div>

  <% @driverby.each do |driverby| %><hr/>
  <div class="text-dark">
    <p> <%= driverby.newdriverby %></p>
    <%= button_to 'Удалить', driverby_path(driverby), method: :delete, data: { turbo_confirm: "Are you sure?" }, method: :delete, class:"btn btn-sm btn-danger", style:"--bs-btn-padding-y: .20rem; --bs-btn-padding-x: .5rem; --bs-btn-font-size: .65rem;" %><p>
  <% end %>
</div>
