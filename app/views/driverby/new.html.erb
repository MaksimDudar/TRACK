<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="field">
        <h5>Добавить BY водителя</h5>
        <%= form_for :driverby, url: {action: "create"} do |f| %>
        <%= f.text_field :newdriverby %>
        <%= f.submit 'OK', class:"btn btn-sm btn-primary rounded-pill" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
