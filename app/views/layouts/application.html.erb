<!DOCTYPE html>
<html>
  <head>
    <title>Слежение</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <div class="container" >
      <div class="row">
        <nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
      <div class="col-lg-3 container-fluid">
        <a class="navbar-brand" href="/">Главная страница</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button></div>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="col-lg-3 navbar-nav">
            <a class="nav-link active" aria-current="page" href="/tracks">Показать все треки</a></div>
            <div class="col-lg-3 navbar-nav">
            <a class="nav-link active" href="/tracks/new">Новое слежение</a></div>
            <div class="col-lg-5 d-grid gap-2 d-md-flex justify-content-md-end">
              <% if user_signed_in? %>
              Привет, <%= current_user.username %><%= button_to "Выход", destroy_user_session_path, method: :delete, class:"btn btn-secondary btn-sm"  %>
              <% else %></div>
              <%= button_to "Вход", new_user_session_path, class:"btn btn-secondary btn-sm"  %>
              <% end %>
            </div>
          </div>
        </div>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>

  </body>
</html>
