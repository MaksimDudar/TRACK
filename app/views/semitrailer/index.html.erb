<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="p-3 mb-2 bg-info text-dark bg-opacity-25">
  <h3>Список полуприцепов</h3></div>

<% @semitrailer.each do |semitrailer| %><hr/>
<div class="text-dark">
<p> <%= semitrailer.newsemitrailer %></p>
<%= button_to 'Удалить', semitrailer_path(semitrailer), method: :delete, data: { turbo_confirm: "Are you sure?" }, method: :delete, class:"btn btn-sm btn-danger", style:"--bs-btn-padding-y: .20rem; --bs-btn-padding-x: .5rem; --bs-btn-font-size: .65rem;" %><p>
<% end %>
</div>
