<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="field">
        <h5>Добавить страну назначения</h5>
        <%= form_for :strana, url: {action: "create"} do |f| %>
        <%= f.text_field :newstrana %>
        <%= f.submit 'OK', class:"btn btn-sm btn-primary rounded-pill" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
